# Railway sets PORT automatically. Keep this only for local development.
PORT=3000

# Required for persistent Railway storage.
# Add a Railway MongoDB service, then copy the MONGODB_URI value into this variable.
MONGODB_URI=mongodb://mongo:PASSWORD@HOST:PORT/railway
